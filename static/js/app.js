(() => {
  'use strict';
  const $ = id => document.getElementById(id);
  const esc = s => String(s ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const LANG = {py:'python',js:'javascript',mjs:'javascript',cjs:'javascript',jsx:'javascript',ts:'typescript',tsx:'typescript',java:'java',c:'c',cc:'cpp',cpp:'cpp',cxx:'cpp',cs:'csharp',go:'go',rs:'rust',php:'php',rb:'ruby',swift:'swift',kt:'kotlin',kts:'kotlin',html:'html',css:'css',json:'json',sql:'sql',md:'markdown',yaml:'yaml',yml:'yaml'};
  const PROFILE = {
    python:['🐍','Python','Tracebacks, tests and AI debugging.','Run'],javascript:['JS','JavaScript','Node execution and browser-aware web preview.','Run'],typescript:['TS','TypeScript','Typed editing, diagnostics and web preview.','Run'],html:['<>','HTML','Live browser workspace.','Run'],css:['#','CSS','Live styling workspace.','Run'],json:['{}','JSON','Validation-focused structured data workspace.','Validate'],sql:['SQL','SQL','Query review and optimization workspace.','Analyze'],java:['☕','Java','Compile and runtime diagnostics.','Run'],c:['C','C','Compiler diagnostics and build/run feedback.','Run'],cpp:['C++','C++','Compiler diagnostics and build/run feedback.','Run'],go:['Go','Go','Build, run and test feedback.','Run'],rust:['🦀','Rust','Compiler and ownership diagnostics.','Run'],php:['PHP','PHP','Runtime diagnostics.','Run'],ruby:['Rb','Ruby','Runtime diagnostics.','Run'],csharp:['C#','C#','.NET-aware editing and diagnostics.','Run'],swift:['Swift','Swift','Compiler/runtime diagnostics.','Run'],kotlin:['Kt','Kotlin','JVM-oriented diagnostics.','Run'],markdown:['M↓','Markdown','Documentation workspace.','Run'],yaml:['Y','YAML','Configuration validation workspace.','Validate'],plaintext:['TXT','Text','Plain text workspace.','Analyze']
  };
  const WEB = new Set(['html','css','javascript','typescript']);
  const EXECUTABLE = new Set(['python','javascript','typescript','php','ruby','go','rust','c','cpp','java','swift']);
  const state = {files:new Map(),active:null,editor:null,monaco:null,detected:null,output:null,analysis:null,history:[],events:[],timer:null,debounce:800,running:false,abort:null,theme:'dark',aiMode:'analyze',aiMessages:[]};
  const sample = `def calculate_total(items):\n    total = 0\n    for item in items:\n        if item is None:\n            continue\n        total += item.price\n    return total\n\nprint(calculate_total([]))\n`;
  function lang(name){return LANG[(name.split('.').pop()||'').toLowerCase()] || 'plaintext';}
  function current(){return state.active ? state.files.get(state.active) : null;}
  function isWeb(){return WEB.has(current()?.language);}
  function setStatus(text,kind='ready'){ $('statusText').textContent=text; $('statusDot').className='status-dot '+kind; }
  function event(type,detail,kind=''){state.events.unshift({time:new Date().toLocaleTimeString(),type,detail,kind});state.events=state.events.slice(0,80);if(document.querySelector('.panel-tab.active')?.dataset.panel==='output')renderPanel('output');}
  function addHistory(label,detail){state.history.unshift({time:new Date().toLocaleTimeString(),label,detail});state.history=state.history.slice(0,40);}
  function renderTabs(){ $('fileTabs').innerHTML=[...state.files.values()].map(f=>`<button class="file-tab ${f.name===state.active?'active':''}" data-file="${esc(f.name)}"><span class="lang">${esc(f.language)}</span><span>${esc(f.name)}</span>${f.dirty?'<span class="dirty">●</span>':''}<span class="x" data-close-file="${esc(f.name)}">×</span></button>`).join(''); }
  function updateContext(){const f=current(); const p=PROFILE[f?.language]||PROFILE.plaintext; const d=state.detected||{}; $('workspaceName').textContent=f?f.name:'Untitled workspace'; $('fileStatus').textContent=f?f.name:'No file'; $('languageIcon').textContent=p[0]; $('languageTitle').textContent=p[1]; $('frameworkText').textContent=d.framework?` · ${d.framework}`:''; $('languageHint').textContent=p[2]; $('languageState').textContent=d.runtime_available===false?'Runtime unavailable':(isWeb()?'Live preview':(f?'Ready':'Ready')); $('runtimePill').textContent=f?(d.framework?`${d.framework} · ${p[1]}`:p[1]):'No file'; $('runBtn').textContent=f?p[3]:'Run'; $('runBtn').disabled=!f; $('downloadBtn').disabled=!f; $('aiContext').textContent=f?`${f.name} · ${p[1]}${d.framework?' · '+d.framework:''}`:'Open a file to give AI context.'; }
  async function detect(){const f=current();if(!f)return;try{const files={};state.files.forEach((v,k)=>files[k]=v.content.slice(0,100000));const r=await fetch('/runtime/detect',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({filename:f.name,files})});state.detected=await r.json();}catch{state.detected=null}updateContext();}
  function setEditorLanguage(){if(!state.editor)return;const f=current();state.monaco.editor.setModelLanguage(state.editor.getModel(),f?.language||'plaintext');}
  function openFile(name,content){const existing=state.files.get(name);const f={name,content,original:existing?.original??content,language:lang(name),dirty:false};f.dirty=f.content!==f.original;state.files.set(name,f);state.active=name;$('emptyState').classList.add('hidden');state.output=null;state.analysis=null;state.events=[];renderTabs();if(state.editor){state.editor.setValue(content);setEditorLanguage();state.editor.focus()}updateContext();detect();updateWeb();if(isWeb())event('Opened',`${name} · live preview ready`);else event('Opened',name);scheduleLive();}
  function openFiles(files){[...files].filter(f=>f.size<=5*1024*1024).forEach(file=>{const r=new FileReader();r.onload=()=>openFile(file.webkitRelativePath||file.name,String(r.result||''));r.readAsText(file)});}
  function sync(){const f=current();if(!f||!state.editor)return false;const v=state.editor.getValue();if(v===f.content)return false;f.content=v;f.dirty=v!==f.original;renderTabs();if(isWeb())updateWeb();return true;}
  function composeWeb(){const f=current();if(!f)return '';let html=f.language==='html'?f.content:'<!doctype html><html><head><meta charset="utf-8"></head><body><main id="bugfalse-app"></main></body></html>';let css='';let js='';state.files.forEach(x=>{if(x.language==='css')css+='\n'+x.content;if(x.language==='javascript'||x.language==='typescript')js+='\n'+x.content});if(f.language==='css')css=f.content;if(f.language==='javascript'||f.language==='typescript')js=f.content;const style=css.replace(/<\/style/gi,'<\\/style');const script=js.replace(/<\/script/gi,'<\\/script');if(/<\/head>/i.test(html))html=html.replace(/<\/head>/i,`<style>${style}</style></head>`);else html=`<style>${style}</style>${html}`;if(script)html=html.replace(/<\/body>/i,`<script>${script}</script></body>`);return html;}
  function updateWeb(){const pane=$('previewPane');if(!isWeb()){pane.classList.add('hidden');return}pane.classList.remove('hidden');const source=composeWeb();$('previewFrame').srcdoc=source;$('previewStatus').textContent='Updated '+new Date().toLocaleTimeString();}
  function scheduleLive(){clearTimeout(state.timer);if(!$('liveToggle').checked)return;const f=current();if(!f)return;state.timer=setTimeout(async()=>{const changed=sync();if(!changed && !isWeb())return;if(isWeb()){updateWeb();event(changed?'Updated':'Refreshed',`${f.name} · live preview refreshed`,'ok');setStatus('Live','ready');return}if(EXECUTABLE.has(f.language)){event('Running',`${f.name} · live execution`);await execute(true)}else if(f.language==='json'){validateJson()}else if(f.language==='yaml'){validateYaml()}},state.debounce);}
  function traceLine(text){const m=String(text||'').match(/(?:line\s+|\.py:|\.js:|\.ts:|\.java:)(\d+)/i);return m?Number(m[1]):null;}
  async function execute(live=false){sync();const f=current();if(!f)return;if(isWeb()){updateWeb();event('Updated',`${f.name} · live preview refreshed`,'ok');return}if(f.language==='json'){validateJson();return}if(f.language==='yaml'){validateYaml();return}if(!EXECUTABLE.has(f.language)){event('Info',`${f.language} is editor-only in this runtime`);setStatus('Editor only','ready');return}state.running=true;$('runBtn').disabled=true;$('stopBtn').disabled=false;setStatus(live?'Live run…':'Running…','running');state.output=null;renderPanel('output');const controller=new AbortController();state.abort=controller;try{const r=await fetch('/execute/',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code:f.content,filename:f.name}),signal:controller.signal});const data=await r.json();if(!r.ok)throw new Error(data.detail||'Execution failed');state.output=data;event(data.ok?'Passed':'Failed',`${f.name} · ${data.duration_ms||0} ms`,data.ok?'ok':'bad');setStatus(data.ok?'Verified':data.runtime_available===false?'Runtime unavailable':'Execution failed',data.ok?'ready':'error');addHistory(data.ok?'Run passed':'Run failed',f.name);renderPanel('output');const line=traceLine(data.stderr);if(line&&state.editor){state.editor.revealLineInCenter(line);state.editor.setPosition({lineNumber:line,column:1});}}catch(e){if(e.name!=='AbortError'){state.output={ok:false,stderr:e.message};event('Error',e.message,'bad');setStatus('Execution failed','error');renderPanel('output')}}finally{state.running=false;$('runBtn').disabled=!current();$('stopBtn').disabled=true;state.abort=null}}
  function validateJson(){const f=current();try{JSON.parse(f.content);state.output={ok:true,stdout:'Valid JSON\n',stderr:''};event('Valid',`${f.name} · JSON parsed successfully`,'ok');setStatus('Valid JSON','ready')}catch(e){state.output={ok:false,stderr:e.message};event('Invalid',`${f.name} · ${e.message}`,'bad');setStatus('Invalid JSON','error')}renderPanel('output')}
  function validateYaml(){const f=current();const bad=/^\s*[^#\n]+:\s*[^#\n]*:\s*[^#\n]*$/m.test(f.content);state.output={ok:!bad,stdout:bad?'':'Basic YAML structure looks valid.\n',stderr:bad?'Possible YAML syntax issue.':''};event(bad?'Review':'Valid',`${f.name} · configuration check`,bad?'bad':'ok');setStatus(bad?'Review YAML':'YAML looks valid',bad?'error':'ready');renderPanel('output')}
  function diffHtml(before,after){const a=before.split('\n'),b=after.split('\n'),n=Math.max(a.length,b.length);let out='';for(let i=0;i<n;i++){if(a[i]===b[i])out+=`<div class="diff-line">  ${esc(a[i]??'')}</div>`;else{if(a[i]!==undefined)out+=`<div class="diff-line removed">- ${esc(a[i])}</div>`;if(b[i]!==undefined)out+=`<div class="diff-line added">+ ${esc(b[i])}</div>`}}return out}
  function renderPanel(name){document.querySelectorAll('.panel-tab').forEach(b=>b.classList.toggle('active',b.dataset.panel===name));const out=state.output||{},r=state.analysis||{};let html='';if(name==='output'){if(isWeb()){html=`<div class="output-head"><span><i class="live-dot"></i>Live updates</span><span>${state.events.length} events</span></div>${state.events.length?state.events.map(e=>`<div class="event ${e.kind}"><span>${esc(e.time)}</span><b>${esc(e.type)}</b><span>${esc(e.detail)}</span></div>`).join(''):'<div class="empty-panel">Edit the file to see live preview updates here.</div>'}`}else html=`<div class="output-head"><span>${out.ok===true?'✓ Success':out.stderr?'✕ Failed':'Ready'}</span><span>${out.duration_ms?esc(out.duration_ms)+' ms':''}</span></div><pre class="terminal">${esc(out.stdout||out.stderr||'Run the current file to see output.')}</pre>`}
    if(name==='problems'){const issues=r.issues||[];html=issues.length?issues.map(i=>`<div class="problem ${esc(i.severity||'warning')}"><span class="sev">${esc((i.severity||'warning').toUpperCase())}</span><div><strong>${esc(i.message||'Issue')}</strong><small>${esc(i.type||'')} ${i.line?'· line '+i.line:''}</small></div><button data-fix="1">Fix</button></div>`).join(''):'<div class="empty-panel"><div>✓<strong>No reported problems</strong><span>Run or analyze the current file.</span></div></div>'}
    if(name==='tests')html='<div class="empty-panel"><div>✓<strong>Tests workspace ready</strong><span>Ask AI to generate tests, then run them in the project runtime.</span></div></div>';
    if(name==='analysis')html=`<div class="analysis"><div class="score">${r.score??'—'}<small>health score</small></div><div><h3>${esc(r.summary?.errors??0)} errors · ${esc(r.summary?.warnings??0)} warnings</h3><p>${esc(r.analysis||'No analysis yet.')}</p></div></div>`;
    if(name==='diff')html=r.fixed_code?`<div class="diff-toolbar"><span>Proposed changes</span><span class="spacer"></span><button id="rejectDiff">Reject</button><button class="apply" id="applyDiff">Apply</button></div><div class="diff">${diffHtml(current()?.content||'',r.fixed_code)}</div>`:'<div class="empty-panel"><div>↔<strong>No AI changes</strong><span>Ask AI to fix or improve the current file.</span></div></div>';
    $('panelContent').innerHTML=html;$('problemCount').textContent=(r.issues||[]).length;
  }
  async function ai(mode=state.aiMode,question=''){const f=current();if(!f){addMessage('ai','Open a file first so I can work with real code.');return}state.aiMode=mode;document.querySelectorAll('.ai-modes button').forEach(b=>b.classList.toggle('active',b.dataset.mode===mode));const q=question||({analyze:'Analyze this file for important bugs, risks and improvements.',fix:'Find the important bugs and propose a safe fix.',improve:'Improve this code while preserving its behavior.'}[mode]||'Help me with this code.');addMessage('user',q);const typing=addMessage('ai','Working…','typing');try{const r=await fetch('/debug/',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code:f.content,filename:f.name,language:f.language,framework:state.detected?.framework||null,mode})});const data=await r.json();typing.remove();if(!r.ok)throw new Error(data.detail||'AI request failed');state.analysis=data;addMessage('ai',formatAI(data));if(data.fixed_code){renderPanel('diff');addHistory('AI proposal',`${mode} · ${f.name}`)}else renderPanel('analysis');setStatus('AI ready','ready')}catch(e){typing.remove();addMessage('ai',`I couldn't complete that request. ${e.message}`);setStatus('AI error','error')}}
  function formatAI(r){let text=r.analysis||r.explanation||r.error||'Analysis complete.';if(r.issues?.length)text+=`\n\n${r.issues.map(i=>`• ${i.severity||'info'}: ${i.message}${i.line?' (line '+i.line+')':''}`).join('\n')}`;if(r.improvements?.length)text+=`\n\nImprovements:\n${r.improvements.map(x=>'• '+x).join('\n')}`;return text}
  function addMessage(role,text,klass=''){const el=document.createElement('div');el.className=`msg ${role} ${klass}`;el.innerHTML=`<div class="role">${role==='user'?'You':'BugFalse AI'}</div><div class="bubble">${esc(text)}</div>`;$('chat').appendChild(el);$('chat').scrollTop=$('chat').scrollHeight;return el}
  function applyAI(){const f=current();if(!f||!state.analysis?.fixed_code)return;f.content=state.analysis.fixed_code;f.dirty=f.content!==f.original;state.editor.setValue(f.content);renderTabs();updateWeb();event('AI applied',`${f.name} · live workspace updated`,'ok');setStatus('AI changes applied','ready');if(isWeb())scheduleLive();else if(EXECUTABLE.has(f.language))setTimeout(()=>execute(true),200);renderPanel('diff')}
  function downloadCurrent(){const f=current();if(!f)return;const blob=new Blob([f.content],{type:'text/plain;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=f.name;a.click();URL.revokeObjectURL(a.href)}
  async function downloadProject(){if(state.files.size<2){downloadCurrent();return}const zip=new JSZip();state.files.forEach(f=>zip.file(f.name,f.content));const blob=await zip.generateAsync({type:'blob'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='bugfalse-project.zip';a.click();URL.revokeObjectURL(a.href)}
  function closeFile(name){state.files.delete(name);if(state.active===name){const next=state.files.keys().next().value;if(next){const f=state.files.get(next);state.active=next;state.editor.setValue(f.content);setEditorLanguage()}else{state.active=null;state.editor.setValue('');$('emptyState').classList.remove('hidden')}}renderTabs();updateContext();detect();updateWeb();renderPanel('output')}
  function initMonaco(){require.config({paths:{vs:'https://cdn.jsdelivr.net/npm/monaco-editor@0.52.2/min/vs'}});require(['vs/editor/editor.main'],()=>{state.monaco=monaco;state.editor=monaco.editor.create($('monaco'),{value:'',language:'plaintext',theme:'vs-dark',automaticLayout:true,fontSize:14,lineHeight:22,fontFamily:'JetBrains Mono,SFMono-Regular,Consolas,monospace',minimap:{enabled:true},padding:{top:12,bottom:12},scrollBeyondLastLine:false,smoothScrolling:true});state.editor.onDidChangeModelContent(()=>{if(!state.active)return;sync();scheduleLive()});state.editor.onDidChangeCursorPosition(()=>{const p=state.editor.getPosition();$('cursor').textContent=`Ln ${p.lineNumber}, Col ${p.column}`})})}
  function init(){initMonaco();renderPanel('output');
    $('openBtn').onclick=()=>$('fileInput').click();$('emptyOpen').onclick=()=>$('fileInput').click();$('fileInput').onchange=e=>{openFiles(e.target.files);e.target.value=''};$('sampleBtn').onclick=()=>openFile('main.py',sample);$('downloadBtn').onclick=()=>state.files.size>1?downloadProject():downloadCurrent();$('runBtn').onclick=()=>execute(false);$('stopBtn').onclick=()=>state.abort?.abort();
    $('liveToggle').onchange=()=>{if($('liveToggle').checked)scheduleLive()};$('debounceSelect').onchange=e=>state.debounce=Number(e.target.value);$('minimapToggle').onchange=e=>state.editor?.updateOptions({minimap:{enabled:e.target.checked}});
    $('settingsBtn').onclick=()=>$('settingsModal').classList.remove('hidden');$('themeBtn').onclick=()=>{state.theme=state.theme==='dark'?'light':'dark';document.body.classList.toggle('light',state.theme==='light');if(state.editor)state.monaco.editor.setTheme(state.theme==='light'?'vs':'vs-dark')};
    $('closeAi').onclick=()=>{$('aiPanel').classList.add('collapsed');$('reopenAi').classList.remove('hidden')};$('reopenAi').onclick=()=>{$('aiPanel').classList.remove('collapsed');$('reopenAi').classList.add('hidden')};
    document.querySelectorAll('.ai-modes button').forEach(b=>b.onclick=()=>ai(b.dataset.mode));document.querySelectorAll('.quick button').forEach(b=>b.onclick=()=>ai('analyze',b.dataset.question));$('sendAi').onclick=()=>{const q=$('aiInput').value.trim();if(!q)return;$('aiInput').value='';const mode=/improv/i.test(q)?'improve':/fix|bug|error|fail/i.test(q)?'fix':'analyze';ai(mode,q)};$('aiInput').onkeydown=e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();$('sendAi').click()}};
    document.querySelectorAll('.panel-tab').forEach(b=>b.onclick=()=>renderPanel(b.dataset.panel));document.addEventListener('click',e=>{const close=e.target.closest('[data-close]');if(close)$(close.dataset.close)?.classList.add('hidden');const fileClose=e.target.closest('[data-close-file]');if(fileClose)closeFile(fileClose.dataset.closeFile);const tab=e.target.closest('[data-file]');if(tab&&!fileClose){const f=state.files.get(tab.dataset.file);if(f){state.active=f.name;state.editor.setValue(f.content);setEditorLanguage();state.output=null;state.analysis=null;state.events=[];renderTabs();updateContext();detect();updateWeb();scheduleLive()}}if(e.target.id==='applyDiff')applyAI();if(e.target.id==='rejectDiff'){state.analysis={...state.analysis,fixed_code:null};renderPanel('diff')}});
    window.addEventListener('dragover',e=>e.preventDefault());window.addEventListener('drop',e=>{e.preventDefault();if(e.dataTransfer.files.length)openFiles(e.dataTransfer.files)});document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key==='Enter'){e.preventDefault();execute(false)}if(e.key==='Escape')document.querySelectorAll('.modal:not(.hidden)').forEach(m=>m.classList.add('hidden'))});
  }
  init();
})();
