This developer-upgrade package contains the inspected/updated backend, CI, tests,
configuration examples, and repository hygiene files. Existing large frontend
assets/templates are not duplicated here because the GitHub connector exposed their
metadata but not all raw contents in this environment. Merge these files into the
current repository rather than treating this ZIP as a byte-for-byte mirror.
